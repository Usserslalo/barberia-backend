# Base de datos (PostgreSQL). Obligatorio.
DATABASE_URL=

# Meta WhatsApp Cloud API. Solo el token aquí; whatsappPhoneId y whatsappWabaId se guardan en LandingConfig (/admin/config).
# WHATSAPP_ACCESS_TOKEN=

# Clave secreta para firmar y verificar JWTs. Obligatorio. Usar una cadena larga y aleatoria en producción.
JWT_SECRET=

# Expiración del access token en segundos. Opcional. Por defecto: 3600 (1h).
# JWT_ACCESS_EXPIRES_IN=3600

# Puerto del servidor. Opcional. Por defecto: 3000.
# PORT=3000

# Origen(es) permitidos para CORS (anti-leeching). Opcional. Por defecto: http://localhost:4200
# Un origen: CORS_ORIGIN=https://jcbarbershop.com
# Varios (separados por coma): CORS_ORIGINS=https://jcbarbershop.com,https://www.jcbarbershop.com
# CORS_ORIGIN=http://localhost:4200

# URL base para imágenes subidas. Opcional. Por defecto: protocolo + host de la petición.
# En producción con reverse proxy: https://api.midominio.com
# BASE_URL=http://localhost:3000

# Dominios permitidos para URLs de imágenes (config, barberos, galería). Separados por coma.
# Si no se define, se usa el host de BASE_URL; en desarrollo se permiten localhost y 127.0.0.1.
# ALLOWED_IMAGE_DOMAINS=localhost,cdn.midominio.com

# Zona horaria para validar "hoy" en citas (available-slots y crear cita). Evita rechazar "hoy" cuando el servidor está en UTC.
# Opcional. Por defecto: America/Mexico_City (ej. México).
# APPOINTMENTS_TIMEZONE=America/Mexico_City
